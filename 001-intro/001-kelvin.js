// Kelvin
const kelvin = 0;
// Celius conversion
const celsius = kelvin - 273;
// Fahrenheit conversion
// Math.floor() to round down to decimal number
var fahrenheit = Math.floor(celsius * (9 / 5) + 32);
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);
